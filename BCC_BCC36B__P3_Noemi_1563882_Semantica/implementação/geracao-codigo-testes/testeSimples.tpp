inteiro soma(inteiro: a, inteiro:b)
	{retorna(a + b)}
	retorna(0)
fim


inteiro principal()
	inteiro: a
	inteiro: b
	inteiro: c

	leia(a)
	leia(b)

	{c := soma(a, b)}

	{escreva(c)}

  retorna(0)
fim

